node_modules/
dist/
.next/
.turbo/
coverage/
*.env
*.env.local
*.env.*.local
.DS_Store
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/
uploads/
# Local-disk document storage (STORAGE_DRIVER=local) — holds uploaded bills
.storage/
*.tsbuildinfo

# Local Windows workaround artifacts — this is a pnpm workspace, so npm
# lockfiles and the packed @ai-accounting/shared tarball are never real inputs.
# They appear when directory symlinks are unavailable and pnpm/npm workspace
# linking has to be bypassed. See CA_CRM_BUILD_PLAN.md.
package-lock.json
packages/shared/*.tgz
